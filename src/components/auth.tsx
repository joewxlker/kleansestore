import { FC } from "react"

interface AuthProps { };

export const Auth: FC<AuthProps> = ({ }): JSX.Element => {
    return (
        <div className=''>

        </div>
    )
}

interface LoginProps { };

export const Login: FC<AuthProps> = ({ }): JSX.Element => {
    return (
        <div className=''>

        </div>
    )
}

interface SignupProps { };

export const Signup: FC<AuthProps> = ({ }): JSX.Element => {
    return (
        <div className=''>

        </div>
    )
}

interface SignupFormProps { };

export const SignupForm: FC<AuthProps> = ({ }): JSX.Element => {
    return (
        <div className=''>

        </div>
    )
}

